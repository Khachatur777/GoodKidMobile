import {StyleSheet} from 'react-native';

export const learnStyles = () =>
  StyleSheet.create({
    // Search
    container: {
      paddingTop: 8,
      paddingBottom: 75,
      gap: 16,
    },
  });
