import {StyleSheet} from 'react-native';

export const homeStyles = () =>
  StyleSheet.create({
    // Search
    container: {
      paddingTop: 8,
      paddingBottom: 75,
      gap: 16,
    },
  });
