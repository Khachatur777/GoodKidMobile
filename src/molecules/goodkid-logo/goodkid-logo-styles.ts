import {StyleSheet} from 'react-native';

export const goodKidLogoStyles = () =>
  StyleSheet.create({
    stackedContainer: {
      alignItems: 'center',
      gap: 10,
    },
    wordmarkContainer: {
      alignItems: 'center',
      gap: 3,
    },
    wordmarkRow: {
      flexDirection: 'row',
      alignItems: 'baseline',
    },
  });
